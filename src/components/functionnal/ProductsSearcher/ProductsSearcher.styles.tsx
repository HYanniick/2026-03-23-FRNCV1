import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  fieldFind: {
    backgroundColor: "#fff",
    padding: 10,
    margin: 10,
    borderRadius: 5,
    fontSize: 16,
    borderColor: "#ccc",
    borderWidth: 1,
  },
});

export default styles;